# ============================================================
# NeoFlow 智能文档处理平台 - Python依赖清单
# ============================================================
# 安装命令：pip install -r requirements.txt
# ============================================================

# ============ Web框架 ============
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.32

# ============ 数据验证 ============
pydantic==2.10.6
pydantic-settings==2.5.2
email-validator==2.2.0

# ============ Supabase ============
supabase==2.11.0
# postgrest 由 supabase 自动管理依赖
realtime==2.5.3

# ============ LangChain & LangGraph ============
langchain==0.2.17
langchain-openai==0.1.25
langchain-core==0.2.43
langgraph==0.2.62
# openai-agents 0.7+ requires openai>=2.9, which conflicts with langchain-openai 0.1.x.
openai-agents==0.2.9
mcp==1.11.0
sse-starlette==3.0.3

# ============ OCR ============
# PaddlePaddle (CPU版本)
paddlepaddle==3.0.0
# 如需GPU版本，使用：
# paddlepaddle-gpu==2.6.2

paddleocr==2.7.3

# ============ 图像处理 ============
opencv-python-headless==4.9.0.80
Pillow==10.2.0
pdf2image==1.17.0

# ============ 异步与HTTP ============
aiofiles==23.2.1
httpx>=0.27.0

# ============ 工具库 ============
python-dotenv==1.0.0
tenacity==8.2.3
openpyxl==3.1.5

# ============ 日志 ============
loguru==0.7.2

# ============ 开发依赖 ============
pytest==7.4.4
pytest-asyncio==0.23.3

# ============ 可选依赖 ============
# Redis缓存支持 (如需要)
# redis==5.0.1
# aioredis==2.0.1

# Celery任务队列 (如需要)
# celery==5.3.6
